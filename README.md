# RaspberryPi OS Setup

Setting up RaspberryPi OS for Microsoft Surface RT

Since the browser does not working use curl to get the required contents

- curl command:
```bash
curl https://raw.githubusercontent.com<username>/<repository>/<branch>/<path/to/file>
```

## Directory Tree

└── Raspos-Setup/
    ├── checkspecs.md
    ├── docker.md
    ├── installations.md
    ├── moltbot.md
    ├── README.md
    └── specs.txt
